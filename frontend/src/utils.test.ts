import{describe,expect,it}from'vitest';import{parseBulkUpstreams}from'./utils';describe('parseBulkUpstreams',()=>{it('supports named and plain URLs',()=>{const rows=parseBulkUpstreams('机场 A | https://a.invalid/sub\nhttps://b.invalid/s\ninvalid',3);expect(rows).toHaveLength(2);expect(rows[0].name).toBe('机场 A');expect(rows[1].name).toBe('上游 4')});it('handles full-width separators',()=>{expect(parseBulkUpstreams('备用｜https://c.invalid/sub')[0].name).toBe('备用')})})
