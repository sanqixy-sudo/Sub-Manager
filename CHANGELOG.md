# 更新记录

## V3.3.2

- 修复关闭自动改名的远程来源仍产生“新增/名称变化待确认”的问题；下一次成功刷新会自动清理历史误报和下游 `⚠️`。
- 手动节点始终不参与自动改名确认流程，节点原名变化不会要求管理员确认，单节点自定义别名仍然优先。
- 订阅组编辑页的手动节点列表新增“当前节点名”，便于按最终下游名称查找节点。
- 保持 SQLite V7、端口 7777、`/data`、组 Token、分类和公开订阅地址不变。
## V3.3.1

- 每条远程订阅链接可单独选择继承组规则、关闭自动改名或使用独立智能规则。
- 当前 V3.3 编辑页补齐上游改名策略控件，并明确显示关闭后的原名透传行为。
- 手动导入节点固定不参与组级自动改名，保留导入名称；单节点自定义别名仍具有最高优先级。
- 保持 SQLite V7、端口 7777、`/data`、组 Token、分类和公开订阅地址不变。

## V3.3.0

- SQLite schema 升级到 V7，新增加密手动节点、测活任务/结果和自定义代理分类。
- 内置 Mihomo v1.19.30，按需通过节点访问 Cloudflare 与 Google 双目标进行真实 HTTPS 测活。
- 新增独立“节点测活”页面、筛选、历史色块、详情记录、全局/组/单节点测试和自动调度。
- 节点来源支持单条或批量 URI、Base64 URI 列表与 Clash/Mihomo `proxies:` YAML，可建立纯手动节点组。
- 手动节点原文和连接指纹不明文落库，使用 `/data/secrets/manual-node.key` 进行 AES-256-GCM 加密。
- 新增 Mihomo 自定义分类，可按整条上游来源自动归类，也可将远程或手动节点单独加入多个分类。
- Mihomo 的 `PROXY` 入口直接包含排序后的自定义分类、自动选择、DIRECT 与全部真实节点，不再生成重复的“全部节点”组；分类变更会使旧输出失效并重生成。
- 修复 VLESS Reality 经 subconverter 生成 V2Ray/Shadowrocket 输出时提示 `No nodes were found`。
- 节点别名允许竖线、国旗、空格等常见符号，仅禁止换行且最长 80 字。
- 公开订阅默认使用 inline 文本显示；只有 `?download=1` 才返回附件下载。
- 保持端口 7777、`/data`、组 Token 和原公开 `/s/{token}/{slug}` 地址不变。

## V3.2.1

- SQLite schema 升级到 V6，新增不透明 HMAC 节点身份、节点偏好和确认状态；升级前自动备份。
- 节点改名升级为“组默认规则 → 单上游覆盖 → 单节点别名”，默认模板分隔符改为 `|`。
- 节点别名会动态追加本次流量和剩余天数；流量/天数变化不会触发名称变化提醒。
- 新增节点和固定名称变化节点在下游加 `⚠️`，持续保留到管理员确认或改名。
- 详情节点页支持行内改名、清除别名、单个确认和批量确认；概览与订阅组列表增加待确认提示。
- 详情概览补齐上游健康、输出状态、最近刷新和待确认入口，并按标签懒加载运维数据。
- 登录页不再预填、暗示或展示默认账号，并尽量关闭浏览器自动填充。
- 增加页面短淡入、骨架屏、请求 singleflight 和局部加载状态，保持端口、`/data`、Token 与公开 URL 不变。
## V3.2.0

- 新增概览、订阅组、运行记录、检查工具和系统设置五页运维导航。
- 新增完整组详情，提供上游、脱敏节点、输出元数据和组级刷新记录。
- SQLite schema 升级到 V5，运行记录保留 30 天且全局最多 1000 条。
- 成功刷新后保存不含服务器、端口、UUID、密码和 URI 的节点名称快照。
- 新增解析与改名沙盒；输入不落盘、不访问新 URL、不生成公开地址。
- 健康接口新增 scheduler 轮询、待刷新组和当前任务状态。
- 设置页按常规、刷新与容错、安全、节点处理拆分，手机端新增底部导航。
- 前端路由按页懒加载，保持现有端口、`/data`、Token 与公开 URL 不变。
- 兼容旧 SQLite 中不带时区的刷新时间，避免健康接口计算待刷新组时失败。

## V3.1.1

- 完全取消向下游透传 `Subscription-Userinfo`，避免多上游额度不一致时显示错误的订阅总流量。
- 兼容已有输出缓存：即使旧 metadata 仍含流量字段，公开响应也不会再发送该 Header。
- 不再把上游套餐流量 Header 写入新的快照或输出 metadata。

## V3.1.0

- 新增订阅组级节点改名，支持原样透传和智能规则两种模式。
- 支持忽略多个入口前缀、国家代码转国旗、可选流量和重置时间识别。
- 支持使用占位符自定义下游名称模板，并在编辑页实时预览。
- SQLite schema 升级到 V4，保留现有组、Token、缓存和公开地址。

## V3.0.1

- 修复 Clash/Mihomo YAML 上游全部正常，但 Mihomo 输出生成失败并导致公开订阅返回 502。
- Mihomo 原生节点现在直接生成配置，不再进行不必要的 subconverter 二次转换。
- 固定规则模板不再依赖运行时访问 GitHub 规则列表。
- 详情抽屉与手动刷新提示现在显示脱敏后的输出失败原因。

## V3.0.0

- 模块化 FastAPI 后端和 Vue 3 管理后台。
- singleflight 刷新、混合订阅解析、last-good 缓存、SQLite V3 迁移和安全接口改版。
